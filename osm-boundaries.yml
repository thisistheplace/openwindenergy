osm-boundaries:
    types:
      - polygons
    select:
      - name
      - boundary
      - admin_level
    where: 
      - (boundary = 'administrative') AND (admin_level IN ('1', '2', '3', '4', '5', '6', '7', '8'))
